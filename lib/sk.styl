ir-sk(url=none, width=none, height=width)
  unless (background == none) { background-image url }
  unless (height == none) { height height }
  overflow hidden
  text-indent 100%
  white-space nowrap
  unless (width == none) { width width }